type User = {
  name: string;
  email: string;
  isActive: boolean;
};

// type myString : string;
// let myName:myString =  "abc";

function createUser(user: User) {
  // need to pass all values
  // return {}
}

createUser({ name: "abc", email: "xyz", isActive: true });
